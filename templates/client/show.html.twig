{% extends 'base.html.twig' %} {% block title %}Client{% endblock %} {% block
body %}
<div class="container col-md-4">
  <h1>Client</h1>

  <table class="table">
    <tbody>
      <tr>
        <th>Prenom</th>
        <td>{{ client.prenom }}</td>
      </tr>
      <tr>
        <th>Nom</th>
        <td>{{ client.nom }}</td>
      </tr>
      <tr>
        <th>Telephone</th>
        <td>{{ client.phone }}</td>
      </tr>
      <tr>
        <th>Emprunt</th>
        <td>{{ client.emprunt }}</td>
      </tr>
      <tr>
        <th>Monnaie</th>
        <td>{{ client.monnaie }}</td>
      </tr>
    </tbody>
  </table>

  <a href="{{ path('client_index') }}" class="btn btn-violet"
    >Aller à la liste des clients</a
  >

  <a href="{{ path('client_edit', { id: client.id }) }}" class="btn btn-yellow"
    >Modifier</a
  >
  <br /><br />
  {{ include("client/_delete_form.html.twig") }}
</div>
{% endblock %}
